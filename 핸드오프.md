# 먹실 작업 핸드오프 (다른 AI 인계용)

> 단일 파일 앱 `먹실_집필작업실.html` 수정 중. 먼저 **`CLAUDE.md`** 와 **`먹실_개발문서.md`** 를 정독할 것. 아래는 이번 세션 요약과 남은 일.

## 구조 핵심 (헷갈리기 쉬움)
- **HTML 1개 = 앱(프로그램)**. **작품 1개 = `.db` 파일 1개** (v0.5.0~). 작업 위치(로컬 폴더 `dirHandle` 또는 Drive 폴더 `cfg.folderId`) 안에 작품마다 `.db`. 파일명 = 작품 제목. 상단 드롭다운 = 그 폴더의 `.db` 목록(선택=파일 로드). 제목 변경 시 파일도 rename. (이전엔 `.db` 1개=여러 작품 서재였음 — 구조 변경됨.)
- 인라인 `onclick` → 전역 `function` 호출. 쓰기는 `run()`/`insert()`만. `innerHTML`엔 `esc()`. 색·폰트는 CSS 변수. (자세한 절대 규칙은 CLAUDE.md)
- 수정할 때마다 `먹실_집필작업실.html`의 `APP_VERSION`/`APP_UPDATED`도 함께 갱신. 첫 화면에 표시됨.
- **개발 단계 = DB 스키마 자유 변경 허용** (지금 `내소설.db` 데이터는 전부 임시). 컬럼 추가/삭제·재생성 OK. 정식 데이터 단계 가면 마이그레이션 루틴 필요.

## 변경 후 검증 (반드시)
```bash
# 문법
python -c "import re;h=open('먹실_집필작업실.html',encoding='utf-8').read();open('app.js','w',encoding='utf-8').write(re.findall(r'<script>(.*?)</script>',h,re.S)[-1])"
node --check app.js && rm -f app.js
# 런타임 스모크: npm i jsdom sql.js@1.12.0 --no-save 후
#   JSDOM(html,{runScripts:'dangerously',url:'https://localhost/'}) 로 로드, jsdomErrors 0 확인
#   db는 win.eval('SCHEMA')로 스키마 생성 후 win.__db 주입 → win.eval('db=window.__db; state.workId=1; ...')
#   끝나면 rm -rf node_modules package-lock.json
```
- 브라우저 실제 확인: `python -m http.server 8000`. (OAuth/Picker/FS 저장은 사람이 확인)

## 이번 세션에 완료한 것
1. **홈에 "작품 수정" 버튼 추가** — `openWorkForm(id)` 편집 경로가 UI에 없던 문제 해결. (`RENDER.dash`)
2. **설정 모달을 그룹(VS Code식)으로 재구성** — `openSettings()`. 그룹 순서: **작품 → 조판 → 저장·Drive**. 작품/조판 그룹은 작품 선택 시에만 표시. CSS `.set-group/.set-head/.set-desc` 추가.
3. **상태·목표 회차·목표 분량을 작품 폼에서 설정의 '작품' 그룹으로 이동** — 변경 즉시 저장(`setWorkSetting`). `saveWork`는 이 3개 컬럼을 안 건드림(보존). 작품 폼엔 안내문구만.
4. **작품별 조판(본문 표시) 설정 신설** — 스키마 `works`에 `editor_preset/editor_font/editor_size/editor_lh/editor_width` 추가, `SCHEMA_VERSION` 1→2.
   - 프리셋 `COMPO_PRESETS`: 기본(serif/17/205/760), 문피아풍(serif/16/140/580), 노벨피아풍(sans/16/160/580), 직접.
   - `setCompoPreset()` 프리셋 적용, `setCompo()` 개별값 수정(→preset '직접'), `applyCompo()` 본문(`#manuscript`)에 인라인 스타일 적용. `openChapter`에서 호출.
   - 조판 양식 출처: 문피아=바탕 10pt/줄간격140%, 노벨피아=고딕 10pt/160% (화면용으로 16px로 키움).
5. **CLAUDE.md 수정** — 스키마 자유 변경(개발 단계) 규칙 반영.
6. **작품별 글자수 기준 설정 신설** — 설정 > 작품에 “글자수는 공백 제외로 계산” 체크박스 추가. 기본 true(공백 제외), 끄면 공백 포함 기준. 스키마 `works.count_no_space` 추가, `SCHEMA_VERSION` 2→3.
   - 회차 목록·대시보드 누적분량·에디터 게이지·저장 델타(`writing_log`)가 현재 작품 설정을 따름.
   - 설정 전환 직후 열린 회차의 `edBaseCount`를 현재 본문 기준으로 재설정해 기존 본문 전체가 새 작성량으로 잡히지 않게 처리.
   - 기존 DB도 `ensureSchema()`가 누락 컬럼을 보강.
7. **Google Picker 설정에 프로젝트 번호(App ID) 추가** — 공식 Picker 예제 기준에 맞춰 설정에 `프로젝트 번호 (Picker App ID)` 입력칸 추가, `pickDriveFolder()`에서 `.setAppId(cfg.appId)` 호출. `먹실_Drive설정.md`도 갱신.
8. **Drive 전용 설정 모달 추가** — `openDriveSettings()` 신설. 시작화면의 `Google Drive로 작업하기`에서 Drive 설정값이 부족하면 일반 설정 대신 Drive 설정만 바로 표시. 일반 설정 헤더에도 `Drive 설정` 바로가기 추가.
9. **첫 화면 앱 버전 표시 추가** — `APP_VERSION`/`APP_UPDATED` 상수 추가. 시작 화면 하단 `#appVersion`에 표시. 문서에 수정 시 버전 갱신 규칙 반영.
10. **Drive 폴더 자동 연결 추가** — `APP_VERSION='v0.3.2'`. Picker API Key 오류를 우회하도록 `Muksil 폴더 자동 연결` 버튼 추가. OAuth 토큰만으로 Drive 최상위 `Muksil` 폴더를 찾거나 생성해 작업 폴더로 설정.

11. **Drive 연동을 Client ID 단일 흐름으로 단순화** — `APP_VERSION='v0.4.0'`. Picker의 `The API developer key is invalid` 오류 원인 제거.
    - **Picker 완전 제거**: `pickDriveFolder()` 함수·`saveDriveCfgThen('folder')` 액션·gapi(`api.js`) 스크립트 삭제. 폴더 연결은 `useDefaultDriveFolder()`(Muksil 폴더 자동 생성/연결)가 유일 경로.
    - 설정 모달에서 **API Key·프로젝트 번호 입력칸 제거**, Client ID 한 칸만 남김. 버튼은 `Drive 연결` + `폴더 연결`(=autoFolder).
    - `openDriveStart()` 게이트를 `clientId`만 검사하도록 수정. `changeStartFolder()`도 자동 연결 호출로 교체.
    - cfg의 `apiKey/appId` 키는 하위호환 위해 잔존하나 미사용. `readCfgInputs()`도 clientId만 읽음.
    - 매뉴얼 2종(`먹실_Drive설정.md`, `먹실_GoogleDrive_연동_매뉴얼.html`) 초보용 4단계로 재작성. 개발문서 §1/§7 갱신.
    - 검증: `node --check` OK, jsdom 스모크 jsdomErrors 0·핸들러 결선 OK·pickDriveFolder 제거 확인.
    - ⚠️ **사람 확인 필요**: 실제 OAuth 동의 + 폴더 자동 생성이 호스팅 환경(`dinosaurroot.github.io/muksil/`)에서 도는지. 단, Cloud 프로젝트에 **Drive API 사용 설정**이 켜져 있어야 함(안 켜면 403).

12. **Drive 스코프 `drive.file` → `drive`(전체)로 확대** — `APP_VERSION='v0.4.1'`. 증상: "Muksil 폴더에서 작업하던 .db를 못 가져옴". 원인: `drive.file`은 앱이 만든 항목만 보여서, Drive 웹에서 수동 생성/업로드한 폴더·파일이 `driveList`/`openDbFromDrive`에 안 잡힘.
    - `DRIVE_SCOPE` 변경. `driveFindRootFolder()`를 `'root' in parents and name=... and mimeType=folder` **타깃 쿼리**로 개선(전체 나열 후 find하던 `driveListRoot()` 제거).
    - 트레이드오프: 동의화면이 "Drive 보기·관리"로 뜸 + 정식 게시 시 Google 검증 필요. **테스트 사용자(본인)면 검증 없이 사용 가능** → 개인용 OK. 기존 사용자는 스코프 확대로 **재동의 1회** 필요.
    - 매뉴얼 2종·개발문서 §1/§7 갱신. 검증: node --check OK, jsdom jsdomErrors 0·driveListRoot 제거 확인.

13. **첫 화면에서 모달/토스트가 안 보이던 z-index 버그 수정** — `APP_VERSION='v0.4.2'`. 증상: splash에서 `Google Drive로 작업하기`(및 설정 등) 클릭해도 "변화 없음". 원인: `#splash` z-index 300인데 `.overlay`(모달)=100, `#toast`=200이라 **splash 뒤에 가려짐**. → `.overlay` 400, `#toast` 500으로 올림(토스트>모달>splash). 앱 진입 후엔 splash가 `.hide`라 원래 문제없었음 = 첫 화면 한정 버그였음.

14. **기본 Client ID를 코드에 내장(기기/브라우저마다 재입력 불필요)** — `APP_VERSION='v0.4.3'`. `DEFAULT_CLIENT_ID` 상수 추가(APP_VERSION 근처). `loadCfg()`가 localStorage에 clientId가 없으면 이 기본값으로 폴백. Client ID는 공개 식별자라 코드 내장 안전(로그인·동의는 본인 계정·테스트 사용자만 통과). 바꾸려면 그 한 줄만 수정. 현재 값은 소유자(dinosaurroot) Client ID.

15. **설정에 "초기화" 버튼 추가(모바일·PC 공통)** — `APP_VERSION='v0.4.4'`. `resetApp()` 신설: confirm 후 토큰 revoke + `localStorage.removeItem('muksil-drive')` + `location.reload()`. 새로고침 시 loadCfg가 기본값(DEFAULT_CLIENT_ID 포함)으로 복구되므로 Client ID는 유지, folderId/folderName/mode/연결만 리셋. `.db` 데이터는 안 건드림. 버튼은 `driveSettingsHtml()` 하단(연결 해제 옆)에 배치.

16. **Drive 연동 매뉴얼 초보용 전면 재작성 + 재사용 가이드 추가** — `먹실_GoogleDrive_연동_매뉴얼.html`. 구조: 0.개념(용어 4개 쉬운말)→준비물→설정5단계(클릭경로+왜 하는지)→실제 OAuth 3화면 클릭순서(계정→계속→계속)→문제해결→체크리스트→**다른 프로젝트 적용 가이드(scope 선택표·함정 모음·결정기록)**→보안. 최신 변경 반영: Client ID 코드 내장(DEFAULT_CLIENT_ID), 초기화 버튼, drive 스코프. `먹실_Drive설정.md`·개발문서 §7.1/7.5/7.6도 동기 갱신(Client ID 재입력 불필요로 정정). 앱 코드 변경 없음(문서만) → index.html 재동기 불요.

17. **저장 구조를 "작품=파일"로 전환** — `APP_VERSION='v0.5.0'`. (사용자 요청) `.db` 1개=서재 → 작품 1개=`.db` 1개.
    - 작업 위치 추상화: 로컬 `dirHandle`(showDirectoryPicker, IndexedDB `muksil-fs`에 영속) / Drive `cfg.folderId`. `hasLocation()`.
    - 신규 스토리지 함수: `listWorkFiles/openWorkFile/makeWorkFile/renameWorkFile/deleteWorkFile`, `sanitizeName/workFileName/titleFromFile`, Drive `driveRenameFile/driveDeleteFile`. `seedSample→seedWork(fields)`.
    - UI: splash 버튼 `📁 이 기기 폴더`/`☁ Drive`. `showWorkList()`(위치의 .db 목록+새 작품). 상단 `#workSelect`=파일 목록(선택=`openWorkFile`, 전환 전 자동저장). `saveWork` 신규=새 파일/수정=제목변경 시 rename. `deleteWork`=파일 삭제. `bootResume()`로 로컬 폴더 권한 살아있으면 자동 재개.
    - 제거: createNewDb/openExistingDb/pickFileFallback/createNewDbOnDrive/openDbFromDrive/loadDriveDb/driveStartOptions/changeStartFolder.
    - 검증: node --check, jsdom+sql.js 스모크(jsdomErrors 0, 함수 결선, seedWork 1작품/1회차, 파일명 새니타이즈) 통과.
    - ⚠️ 사람 확인 필요: 실브라우저에서 폴더 선택→작품 생성/전환/제목변경(rename)/삭제, Drive 동일.

18. **저장 시 gzip 압축 (항상 압축)** — `APP_VERSION='v0.5.1'`. 파일명 `.db`→`.db.gz`. `CompressionStream`/`DecompressionStream` 내장 사용. 헬퍼: `gzipBytes/gunzipBytes/isGzip/exportBytes/importBytes`. 저장 경로(writeToFile/makeWorkFile/renameWorkFile)는 `exportBytes()`(gzip), 읽기(openWorkFile)는 `importBytes()`(매직바이트 `1f 8b` 자동감지 → 구 `.db`도 그대로 열림). `workFileName→.db.gz`, `titleFromFile`은 `.db.gz`/`.db` 둘 다 strip, `isWorkFile`로 목록 필터. CompressionStream 미지원 시 미압축 폴백. 임계값 분기 없음(사용자 요청). 검증: 라운드트립(시드→압축→매직→해제→복원) 데이터 무손실 확인.

19. **레거시 `.db` → `.db.gz` 자동 마이그레이션** — `APP_VERSION='v0.5.2'`. 증상: 압축 전(v0.5.0) 만든 파일이 Drive에 `.db`로 보임 + 재저장 시 내용만 gzip되고 이름은 `.db` 유지(이름/내용 불일치). `openWorkFile`에서 이름이 `.db`(비 `.db.gz`)면 충돌 없을 때 `renameWorkFile`로 `.db.gz`로 자동 정리. 이후 일관되게 `.db.gz`.

20. **작품 삭제 = 완전삭제 → '휴지통' 폴더로 이동(소프트 삭제)** — `APP_VERSION='v0.5.3'`. 작업 폴더(Muksil 등) 안에 `휴지통` 하위폴더 자동 생성 후 그리로 이동. `제목 (삭제 YYYY-MM-DD HHMM).db.gz`로 보관(충돌 방지). 신규: `trashWorkFile`, `driveMoveToFolder`(addParents/removeParents PATCH), `driveEnsureSubfolder`, `archivedName`, `TRASH_NAME='휴지통'`. `deleteWork`가 `deleteWorkFile`→`trashWorkFile` 호출(완전삭제 함수는 잔존·미사용). 폼 버튼 '🗑 휴지통으로'. 휴지통 폴더는 작품 목록에서 자동 제외(폴더/하위경로). 되살리기는 그 폴더에서 파일을 작업 폴더로 다시 옮기면 됨(앱 내 복원 UI는 미구현).

21. **세계관에 카테고리 칩(화면 내 다층 메뉴) 추가** — `APP_VERSION='v0.6.0'`. (방식 A 채택) `RENDER.lore` 상단에 `전체`+존재 카테고리 칩(개수 배지). 칩 클릭=`setLoreCat(cat)`→그 카테고리만, `전체`=카테고리별 섹션 모두. `state.loreCat`(기본 '전체') 추가. 빈/사라진 카테고리는 자동 `전체` 폴백. 새 설정 추가 시 현재 선택 카테고리를 기본값으로(`openLoreForm`). CSS `.cat-chips/.cat-chip`(가로 스크롤, 모바일 대응). LORE_CATS 밖 사용자정의 카테고리도 칩에 표시(미래 대비). `loreSection()` 헬퍼 분리. **확장 여지**: 같은 패턴 플롯(아크)/인물(역할)에 적용 가능.

22. **세계관 카테고리를 DB 테이블화 + 설정에서 추가/삭제** — `APP_VERSION='v0.6.0'`, `SCHEMA_VERSION 3→4`. 신규 테이블 `lore_cats(id,work_id,name,sort)`. `LORE_CATS`(고정) → `DEFAULT_LORE_CATS`(시드용 기본 7개) + `loreCatList()`(동적 조회). `seedWork`가 신규작품에 기본 카테고리 시드, `ensureSchema`가 기존 .db에 비면 백필(CREATE TABLE IF NOT EXISTS + 빈 경우 채움). 세계관 칩(`RENDER.lore`)·`openLoreForm` select가 `loreCatList()` 사용. 설정 '작품' 그룹에 카테고리 편집 UI(`addLoreCat`/`deleteLoreCat`, CSS `.set-sub/.cat-edit/.cat-edit-chip`). 삭제 시 그 카테고리 lore는 `미분류`로 이동(데이터 보존). 검증: jsdom — 시드7/추가/빈삭제/항목있는삭제→미분류 이동/칩 반영 OK.

23. **카테고리 시스템 일반화(세계관+플롯+인물) + 순서변경·이름변경** — `APP_VERSION='v0.7.0'`, `SCHEMA_VERSION 4→5`. `lore_cats` → 공통 `cats(work_id,kind,name,sort)` (kind=lore/plot/char). `CAT_KINDS` 맵으로 테이블·컬럼·기본값 일반화. 함수: `catList/seedCats/backfillCats/catAdd/catRename/catMove/catDelete/catEditorHtml/setCat/catChipsHtml/catData`. 세 화면(RENDER.lore/plot/cast) 모두 상단 칩 필터로 통일, 폼 select도 `catList(kind)`. 설정 '작품' 그룹에 3개 편집기(행 레이아웃: 이름클릭=rename, ↑↓=정렬, ✕=삭제). 삭제 시 항목→'미분류', 이름변경 시 항목 분류값 동기화. `backfillCats`가 레거시 lore_cats·기존 arc/role distinct값 흡수. `state.plotArc/charRole` 추가. CSS `.cat-rows/.cat-row`. 검증: jsdom — 3종 시드/칩/카운트/add/move/rename/delete(미분류) OK.

## 남은 일 (미적용)
### A. 기타 점검에서 발견된 "코드엔 있으나 UI에 없는" 항목 (사용자 판단 대기)
- **시놉시스**: 입력만 되고 어디에도 표시 안 됨.
- **회차 메모**: 저장되나 회차 목록에 "메모 있음" 표시 없음.

## 사용자 선호 (말투/방식)
- 한국어 캐주얼 존댓말, 짧게. 큰 변경 전 핵심만 확인하고 진행.
- `AskUserQuestion` 도구보다 대화로 질문하는 걸 선호함.
